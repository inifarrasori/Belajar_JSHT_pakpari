/* CATATAN 
-   urutan inisialisasi variabel, pastikan inisialisasi sesuai top-down
-   && [AND] dan || [OR] sebagai operator (sering dipakai pada if else)
-   git init, add, commit, branch(cek branch)/switch(ganti branch aktif / buat baru), remote add origin (sinkron web), 
-   push(masuk ke github), fetch(ambil info dari web), merge(update lokal dengan info fetch)

-   REREAD git merge
*/